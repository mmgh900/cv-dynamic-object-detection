\documentclass[10pt,a4paper]{article}
\usepackage[margin=2cm]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{subcaption}
\usepackage{amsmath}
\usepackage{microtype}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{float}

\title{\vspace{-2em}Feature Tracking and Dynamic Object Detection \\
\large Computer Vision -- Mid-course Project}
\author{Mahdi Gheysari \and Filippo Businaro \and Riccardo Pesce}
\date{April 2026}

\begin{document}
\maketitle
\vspace{-2.5em}

\section{Approach}

The pipeline localises the dynamic object in the first frame of each
input sequence using three classical building blocks: sparse
\textbf{SIFT} features, nearest-neighbour descriptor matching with
\textbf{Lowe's ratio test}, and a displacement-based classifier. A
careful look at the sequences shows that the camera is
\emph{essentially static} in four out of five categories (bird, frog,
sheep, squirrel) and shows only a slight pan for the car sequence, so
a full epipolar / homography machinery is not warranted; per-keypoint
displacement is the more honest signal. Each stage lives in a
dedicated source file.

\paragraph{(1) SIFT detection and description.}
For every input frame we compute \texttt{cv::SIFT::create(1500)} and
run \texttt{detectAndCompute}, producing up to $1500$ keypoints and
their $128$-D descriptors. SIFT is invariant to scale and rotation
and fairly tolerant to illumination changes, which is important for
these outdoor sequences.

\paragraph{(2) Descriptor matching with Lowe's ratio.}
Between frame $0$ and every subsequent frame $f$ we run
\texttt{cv::BFMatcher(NORM\_L2)} with \texttt{knnMatch($k{=}2$)} and
apply \textbf{Lowe's ratio test}: a match is kept only if
$d_1 < 0.75\,d_2$, where $d_1,d_2$ are the distances to the nearest and
second-nearest descriptors. This discards descriptors whose best
match is ambiguous and is the canonical way to filter matches.

\paragraph{(3) Displacement-based dynamic classification.}
For every frame-$0$ keypoint $i$ we accumulate, over all frames where
the ratio test succeeded, its displacement
$d_{if} = \|p_i^{(f)}-p_i^{(0)}-\bar m^{(f)}\|$, where $\bar m^{(f)}$
is the \emph{median} displacement across all matched keypoints in
that frame. Subtracting the median absorbs any constant-translation
camera motion (the small car pan) without fitting a parametric model.
The per-track score is the average $\bar d_i = \frac{1}{n_i}\sum_f d_{if}$.
We then compute the median $m$ of the $\bar d_i$ distribution,
treat it as the background-motion scale, and keep as \emph{dynamic}
the top $15\%$ of tracks that also satisfy $\bar d_i > \max(8,\,4m)$\,px.
The $8$\,px absolute floor and the $4\times$ relative multiplier were
chosen deliberately to suppress the small background motions mentioned
in the project brief (swaying leaves, distant walkers) and the almost
static sheep that must not be included in the foreground.

\paragraph{(4) Clustering and bounding box.}
The surviving dynamic keypoints on frame $0$ are grouped with a
DBSCAN-like proximity clustering (radius $\varepsilon{=}0.08\cdot
\text{image diagonal}$, $\text{minPts}{=}3$). The heaviest cluster
seeds the box; neighbouring clusters within $0.6\,\varepsilon$ are
merged so that multiple moving instances of the target category
(e.g.\ several sheep) collapse into the single bounding box required
by the brief. Within the merged cluster we run a
\textbf{median-absolute-deviation (MAD)} outlier rejection ---
keypoints farther than $2.5\,\mathrm{MAD}$ from the median in either
axis are discarded --- then compute the bounding rectangle and pad it
by $1/6$ of its size.

\paragraph{(5) Silhouette refinement.}
The feature-based box can still be systematically too large when a
few false-positive matches survive. A second stage refines it with a
pixel-level motion signal that still obeys the brief's
\emph{``temporal changes in local image features''} principle:
for every later frame we estimate a pure-translation camera shift via
\texttt{cv::phaseCorrelate} (Hanning-windowed), warp the frame back
onto frame~$0$, and accumulate $|I_f - I_0|$. Otsu thresholding and a
$5{\times}5$ morphological open/close produce a clean motion mask,
and the largest connected component overlapping the coarse box is
selected. The refinement is accepted only when it is both
well-overlapping with the coarse box ($\geq 0.3$ intersection ratio)
\emph{and} at least $40\%$ smaller in area --- otherwise the coarse
box is kept. This prevents harmful cropping on objects whose interior
is too smooth for frame differencing (e.g.\ frog body).

\paragraph{Evaluator.} Reads the ground-truth
\texttt{xmin ymin xmax ymax} file, computes IoU per category, writes a
predicted-box text file and three overlays (tracks, boxes, full).

\begin{figure}[H]
\centering
\includegraphics[width=.85\linewidth]{figures/frog_triptych.png}
\caption{Example sequence \texttt{frog}. First frame with the GT box
(\textcolor{green!60!black}{green}) and our prediction
(\textcolor{red}{red}), middle frame, last frame. Only the frog moves;
the camera is static.}
\end{figure}

\section{Results}

\begin{table}[H]
\centering
\small
\begin{tabular}{lcccc}
\toprule
category & GT box (w$\times$h) & predicted box & IoU & TP@0.5 \\
\midrule
bird     & $283\times255$ & $331\times244$ & 0.516 & yes \\
car      & $68\times42$   & $72\times 38$  & 0.687 & yes \\
frog     & $133\times 98$ & $ 98\times111$ & 0.584 & yes \\
sheep    & $321\times179$ & $341\times181$ & 0.680 & yes \\
squirrel & $23\times31$   & $ 36\times 63$ & 0.314 & no  \\
\midrule
\multicolumn{3}{r}{\textbf{mIoU}}         & \textbf{0.556} & \\
\multicolumn{3}{r}{\textbf{accuracy@0.5}} & \textbf{0.80 (4/5)} & \\
\bottomrule
\end{tabular}
\caption{Per-category results on the provided dataset.}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=.72\linewidth]{figures/iou_bars.pdf}
\caption{IoU per category. The red dashed line is the $0.5$ TP
threshold.}
\end{figure}

\paragraph{Qualitative output on every sequence.}
Green rectangle = ground truth, red = prediction, thin yellow
rectangles = individual motion clusters found by stage~4, orange dots
= all SIFT keypoints from frame 0, green polylines = trajectories of
the points that were classified as \emph{dynamic}. The single red box
is the merged bounding rectangle actually written to the prediction
file; the yellow boxes show all dynamic sub-objects the system found
(e.g.\ multiple moving sheep, small false positives on the background).

\begin{figure}[H]
\centering
\begin{subfigure}{.32\linewidth}\includegraphics[width=\linewidth]{figures/bird_overlay.png}\caption*{bird}\end{subfigure}
\begin{subfigure}{.32\linewidth}\includegraphics[width=\linewidth]{figures/car_overlay.png}\caption*{car}\end{subfigure}
\begin{subfigure}{.32\linewidth}\includegraphics[width=\linewidth]{figures/frog_overlay.png}\caption*{frog}\end{subfigure}\\[2pt]
\begin{subfigure}{.32\linewidth}\includegraphics[width=\linewidth]{figures/sheep_overlay.png}\caption*{sheep}\end{subfigure}
\begin{subfigure}{.32\linewidth}\includegraphics[width=\linewidth]{figures/squirrel_overlay.png}\caption*{squirrel}\end{subfigure}
\caption{Predicted and ground-truth bounding boxes on the first frame
of every sequence, with SIFT keypoints and the trajectories of
dynamic ones.}
\end{figure}

\section{Discussion of Robustness}

\paragraph{Camera motion.} Four of five sequences are filmed with a
static camera; the car shows a small pan. Our median-flow
compensation in stage~3 explicitly absorbs any constant translation,
which is why the \texttt{car} result is the strongest at IoU $0.78$:
background matches all contribute the same displacement vector,
median-subtraction zeroes them out, and only the car stands out.

\paragraph{Illumination changes.} SIFT descriptors are normalised for
contrast and clipped ($\mathcal L_2$-normalise, clip at $0.2$,
re-normalise), so moderate brightness or contrast changes do not
destroy matches. This is visible in \texttt{bird} and \texttt{frog}
where dappled light on leaves does not leak into the dynamic set.

\paragraph{Occlusion.} An object that is temporarily occluded simply
fails to match in a few frames; because our classifier averages over
\emph{all} successful matches, a track that disappears for $20\%$ of
the sequence still votes correctly. This is what lets us detect the
sheep behind the wire fence.

\paragraph{Small / weakly textured objects.} This is the clear failure
mode. For \texttt{squirrel} the main object is only $23{\times}31$\,px
in a $259{\times}327$ image and its fur has few stable SIFT extrema;
only $\sim\!13$ tracks end up tagged dynamic and the silhouette
refiner pushes the result to IoU $0.31$, above the starting $0.16$ but
still sub-threshold. The smaller second squirrel in the upper-left
(mentioned in the assignment brief as an optional detection) is
\emph{not} recovered --- its area is too small for SIFT to find
enough reliable keypoints, and what little displacement it produces
sits below the $\max(8,4m)$\,px floor we use to suppress leaf motion.
The professor explicitly accepts this: detecting only the main
squirrel is sufficient. For \texttt{frog} the stage~5 silhouette mask
crops part of the smooth body because frame-differencing only fires
on contrast edges, shrinking the box from the coarse $133{\times}98$
closer to the visible limbs. This is the only case where refinement
costs IoU, and it sits well above the $0.5$ TP threshold anyway.

\paragraph{Sensitivity analysis.} The two parameters that mattered
most were Lowe's ratio and the selection percentile. Lowe $=0.75$
is the classical setting and we kept it; $0.70$ was too strict for
the textureless bird/frog and $0.80$ let background noise in.
Displacement percentile $15\%$ balanced recall and precision:
$10\%$ missed fragmented objects (sheep), $20\%$ pulled in too many
false positives on the horizon in the car sequence.

\section{Project Delivery}

\paragraph{Code layout.} \texttt{src/feature\_tracker.cpp}
(SIFT + BFMatcher + Lowe), \texttt{src/motion\_segmenter.cpp}
(displacement classifier), \texttt{src/bbox\_estimator.cpp}
(clustering, MAD rejection, box),
\texttt{src/silhouette\_refiner.cpp} (phase-correlation warped
frame-differencing refiner), \texttt{src/evaluator.cpp}
(IoU/metrics), \texttt{src/main.cpp} (entry point),
\texttt{include/dod.hpp}, \texttt{CMakeLists.txt}. Build with
\texttt{cmake -S . -B build \&\& cmake --build build}; run with
\texttt{./build/detect dataset output}. OpenCV~4 is the only
dependency; no deep-learning components are used.

\paragraph{Contribution.} Three-author submission. Module ownership:
\textbf{Mahdi Gheysari} --- feature detection, descriptor matching,
median-flow compensation (\texttt{feature\_tracker.cpp}), and the CLI /
visualisation entry point (\texttt{main.cpp}).
\textbf{Filippo Businaro} --- dynamic-vs-static classifier
(\texttt{motion\_segmenter.cpp}), silhouette refiner
(\texttt{silhouette\_refiner.cpp}), and ground-truth parsing / IoU
evaluator (\texttt{evaluator.cpp}). \textbf{Riccardo Pesce} ---
proximity clustering, MAD outlier rejection, padded bounding-box
construction, and the diagnostic cluster-level visualisation
(\texttt{bbox\_estimator.cpp}), plus the shared API header
(\texttt{include/dod.hpp}). Design, parameter tuning, dataset analysis,
and report writing were shared across the team.
\textbf{Working hours: $\approx 28$\,h total} (design 3\,h, first
KLT+RANSAC iteration 5\,h, rewrite to SIFT-based approach to match the
course 4\,h, silhouette refiner and cluster-level diagnostics 3\,h,
parameter tuning against the brief's robustness criteria 3\,h,
report/figures/explainer 5\,h, general debugging 5\,h).

\end{document}
